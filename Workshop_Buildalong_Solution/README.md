# Workshop Build-Along Solution

Completed reference for Slides 10-26.

It includes Header, TaskCard, AddTaskForm, add/delete/toggle handlers, live counters, and the `SummaryBar` Copilot demo component.

```bash
npm install
npm run dev
```
